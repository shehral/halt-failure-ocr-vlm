# Pinned dependencies for the halt-failure-ocr-vlm reproducibility repo.
#
# Model under study:
#   model id        : nanonets/Nanonets-OCR2-3B  (Qwen2.5-VL-3B-Instruct fine-tune)
#   pinned revision : c3886ff00bb037ce7da24988c9eafaf1fe2bed72
#   architecture    : 36 decoder layers, hidden 2048, 16 attention heads, 2 KV heads
#
# Loader (mandatory lm_head tie-fix):
#   The public checkpoint omits `lm_head.weight` and relies on `tie_word_embeddings`.
#   transformers 5.x reads that flag off the OUTER config (False) instead of `text_config`
#   (True), so `tie_weights()` becomes a no-op, `lm_head` stays at meta-init zeros, and
#   greedy generation collapses to "!" forever. Every loader MUST do, before `.to(device)`:
#
#       from transformers import AutoModelForImageTextToText, AutoProcessor
#       model = AutoModelForImageTextToText.from_pretrained(
#           "nanonets/Nanonets-OCR2-3B",
#           revision="c3886ff00bb037ce7da24988c9eafaf1fe2bed72",
#       )
#       model.config.tie_word_embeddings = True
#       model.tie_weights()
#       assert model.lm_head.weight.data_ptr() == model.model.language_model.embed_tokens.weight.data_ptr()
#       model = model.to(device)
#
# Install:  pip install -r environment/requirements.txt
# Python pinned to the 3.13 line (matches the captured environment).

# --- core numerics ---
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
joblib==1.5.3
threadpoolctl==3.6.0

# --- deep learning ---
torch==2.12.0
torchvision==0.27.0
accelerate==1.13.0
transformers==5.8.1
tokenizers==0.22.2
safetensors==0.7.0
huggingface-hub==1.14.0
hf-xet==1.5.0
sentencepiece==0.2.1
regex==2026.5.9

# --- interpretability ---
nnsight==0.7.0

# --- datasets / vision-language inputs ---
datasets==4.4.0
qwen-vl-utils==0.0.14
pillow==12.2.0
av==17.0.1

# --- plotting ---
matplotlib==3.10.9
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
pyparsing==3.3.2

# --- support / serialization ---
networkx==3.6.1
sympy==1.14.0
mpmath==1.3.0
filelock==3.29.0
fsspec==2026.4.0
jinja2==3.1.6
markupsafe==3.0.3
packaging==26.2
pyyaml==6.0.3
protobuf==7.34.1
psutil==7.2.2
requests==2.34.2
tqdm==4.67.3
typing-extensions==4.15.0
pydantic==2.13.4
